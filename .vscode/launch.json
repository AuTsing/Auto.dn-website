{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "VitePress dev",
            "request": "launch",
            "type": "chrome",
            "url": "http://localhost:5173/",
            "preLaunchTask": "${defaultBuildTask}"
        }
    ]
}
